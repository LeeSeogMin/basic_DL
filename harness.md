# 딥러닝 기초 강의자료 하네스

## 1. 이 하네스가 지금 막는 것

하네스는 "하겠다"는 규칙을 "했다"는 증거로 바꾸는 장치다. 산문으로 적은 지시는 지켰는지 확인할 수 없다. 파일과 종료코드는 확인할 수 있다.

이 저장소의 국면이 한 번 바뀌었다. 교재 15장 작성은 2026-08-13에 끝났고(`.ai/context.md` 완료 현황), 지금은 학기 중이다. 그래서 막아야 할 위험이 달라졌다.

| 국면 | 주된 위험 | 하네스가 하는 일 |
|---|---|---|
| 작성기 (~2026-08-13) | 실행하지 않은 결과를 본문에 쓰는 것 | 실행 로그와 증거 JSON을 만들게 강제 |
| **운영기 (2026-09-01~)** | **고친 뒤 뒤따라야 할 것을 안 고치는 것** | **코드·본문·PDF 세 가지가 어긋나면 실패로 만든다** |

운영기의 어긋남은 세 가지 모양으로 나타난다.

1. 실습 코드를 고치고 결과 로그를 다시 만들지 않는다. 본문 수치가 옛 코드의 것으로 남는다.
2. 본문을 고치고 PDF를 다시 만들지 않는다. 학생이 받는 PDF와 저장소의 본문이 달라진다.
3. 교사 기계에서만 도는 코드를 배포한다. 학생이 실행하면 실패한다.

## 2. 절대 규칙

- 실행하지 않은 코드 결과를 본문에 쓰지 않는다. 임의 숫자, 가짜 출력, "예시 결과"를 실제 실행 결과처럼 쓰지 않는다.
- 본문의 수치, 정확도, 손실값, 표본 수, 출력 예시는 `practice/chapter{N}/results/` 의 로그와 결과 파일에서만 가져온다.
- 인용한 뒤에는 ``_출처: `practice/chapter{N}/results/파일명.log`_`` 를 붙인다. 게이트가 이 표기를 읽어 파일이 실제로 있는지 확인한다.
- 합성 데이터로 만든 결과는 본문에 합성 데이터라고 밝힌다. "실측"이라고 쓰지 않는다.
- 참고문헌, 논문, URL, DOI는 확인한 것만 쓴다.
- 요청 범위 밖의 문제를 발견하면 고치지 말고 보고한다.

## 3. 학기 중 개정 루프

장 하나를 고칠 때 다섯 단계를 순서대로 밟는다. 앞 단계를 건너뛰면 뒤 단계의 게이트가 실패한다.

| 단계 | 하는 일 | 확인 명령 |
|---|---|---|
| 1 | 무엇을 왜 고치는지 정한다. 고칠 파일을 연다 | — |
| 2 | 실습 코드를 고쳤으면 다시 실행해 증거를 새로 만든다 | `python3 scripts/run_and_capture.py {N}` |
| 3 | 본문 수치를 새 로그에 맞춘다 | `python3 scripts/run_and_capture.py {N} --verify` |
| 4 | 본문을 고쳤으면 PDF를 다시 만든다 | `python3 scripts/md2pdf.py docs/ch{N}.md` |
| 5 | 게이트를 통과시킨 뒤 커밋한다 | `./scripts/harness.sh` |

실습 코드를 건드리지 않고 설명 문장만 고쳤다면 2·3단계는 건너뛴다. 4단계는 건너뛰지 않는다. 본문을 한 줄만 고쳐도 PDF는 낡는다.

## 4. 게이트

### 4-1. 자동 게이트 세 가지

`scripts/course_gates.py`가 세 가지를 검사한다. 검사 로직은 새로 쓰지 않고 `scripts/run_and_capture.py`의 것을 그대로 쓴다.

| 게이트 | 무엇을 비교하는가 | 실패할 때의 뜻 |
|---|---|---|
| G1 실행 증거 무결성 | 실습 코드 파일의 sha256 해시 ↔ `results/*.evidence.json`에 적힌 `source_sha256` | 코드를 고치고 다시 실행하지 않았다 |
| G2 본문 인용 로그 실존 | `docs/ch*.md`의 `_출처:` 표기가 가리키는 경로 ↔ 실제 파일 | 본문이 없는 파일을 근거로 대고 있다 |
| G3 md-PDF 동기 | 본문 마크다운의 현재 해시 ↔ `docs/ch{N}.pdf.evidence.json`에 적힌 생성 당시 해시 | 본문을 고치고 PDF를 다시 만들지 않았다 |

G3에는 전제가 하나 있다. git은 파일을 고친 시각을 보존하지 않아서, 클론한 저장소에서는 시각을 비교해도 낡았는지 알 수 없다. 그래서 `scripts/md2pdf.py`가 변환할 때마다 `docs/ch{N}.pdf.evidence.json`에 원본 해시를 남긴다. 그 기록이 없는 PDF는 검사 대상에서 빠지고 "생성 기록 없음"으로 보고된다. 다음에 그 장의 PDF를 다시 만들면 기록이 생긴다.

2026-09-16 기준 검사 규모는 실습 코드 37개 파일, 본문 인용 64건, PDF 2개(둘 다 생성 기록 없음)다.

### 4-2. 수동 확인: 학생 환경에서 도는가

이것은 전 장 재실행이 필요해서 자동 게이트에 넣지 않았다. 학기 중 배포 전이나 실습 주차 전에 사람이 판단해서 실행한다.

```bash
python3 scripts/run_and_capture.py {N}
```

확인할 것은 네 가지다. CPU만으로 끝나는가. `practice/requirements.txt`의 패키지만 쓰는가. 데이터 내려받기가 실패하면 원인을 출력하는가. 난수 seed가 고정되어 있는가.

## 5. 실행 방법

```bash
# 전체 게이트 (커밋 전)
./scripts/harness.sh

# 빠른 검증 (PDF 재생성 없이 자주)
./scripts/verify.sh

# 게이트만 따로, 통과 항목까지 보려면
python3 scripts/course_gates.py --verbose
python3 scripts/course_gates.py --gates g1,g2

# 장별 실행 증거
python3 scripts/run_and_capture.py 5
python3 scripts/run_and_capture.py 5 --file 5-1-one-neuron.py
python3 scripts/run_and_capture.py 5 --verify

# 본문을 PDF로
python3 scripts/md2pdf.py docs/ch5.md
```

`./scripts/harness.sh`는 성공 시 마지막 줄에 `HARNESS_PASS`, 빠른 검증은 `VERIFY_PASS`를 출력한다.

**`HARNESS_PASS`는 검사를 하나 이상 실제로 수행했을 때만 나온다.** 검사를 하나도 못 했으면 `HARNESS_FAIL: 검사를 하나도 수행하지 못했다`로 끝난다. 예전에는 `ruff`와 `pytest`가 없는 기계에서 둘 다 건너뛴 뒤 `HARNESS_PASS`가 나왔다. 그 값을 완료 조건으로 삼는 문서가 세 곳(`AGENTS.md` §3, `CODEX.md` 완료 기준, `.ai/common.md` 완료 기준)이어서, 완료 판정이 무조건 통과했다.

## 6. 산출물 규격

`docs/ch{N}.md`의 구조 규격은 `.ai/chapter-spec.md`가 단일 원천이다. 이 문서는 규격을 다시 적지 않는다. 형식이 헷갈리면 `docs/ch4.md`를 그대로 따른다.

8주차와 15주차는 교재 장이 아니다. 8주차 안내는 `docs/ch8.md`에 둔다.

반복해서 밟는 함정은 `.ai/gotchas.md` 하나에 모았다. 한 번 푼 문제의 기록은 `.ai/lessons-learned.md`에 남긴다. 두 파일이 무엇이고 왜 필요한지는 `agent-harness-guide.md`에 학생용으로 설명해 두었다.

## 7. 실패 시 행동 규칙

1. 누락된 산출물을 명시하고 다음 단계로 진행하지 않는다.
2. 실행 로그가 없으면 본문 수치를 쓰지 않는다.
3. `[stale]`이 나오면 코드를 다시 실행한다. 증거 JSON을 손으로 고치지 않는다.
4. 코드가 실패하면 실패 로그를 남기고 원인을 고친다. 실패를 감추지 않는다.
5. 게이트를 못 돌린 채 완료를 보고하지 않는다. 못 돌렸으면 무엇을 왜 못 돌렸는지 적는다.
6. 요청 범위 밖의 문제는 고치지 말고 보고한다.

## 8. 이번 개정에서 뺀 단계 (2026-09-16)

뒤에 읽는 사람이 "빠뜨린 것"과 "일부러 뺀 것"을 구별할 수 있게 남긴다.

| 뺀 단계 | 뺀 이유 |
|---|---|
| Source Review 단계의 `docs/curri.md` 확인 | 그 파일이 없다. `AGENTS.md` §1이 "두지 않는다"고 정했고, 내용은 `강의계획서.md`와 `contents.md`로 나뉘었다 |
| Research 단계의 `content/research/ch{N}-*.md` | `content/` 아래에 파일이 하나도 없다. 리서치 메모는 `schema/chap{N}.md`에 들어가고 있다 |
| Lecture Material 단계의 `lecture/chapter{N}.md` | `lecture/` 디렉터리가 없다. 교재 본문은 `docs/ch{N}.md` 하나로 모였다 |
| Planning ~ Verification 7단계 워크플로우 | 장을 처음 쓰는 절차였다. 15장 작성이 끝나 더 이상 밟지 않는다. §3의 개정 루프가 대신한다 |

장을 새로 만들 일이 생기면 `.ai/chapter-spec.md`의 §1 산출물 표를 따른다.
